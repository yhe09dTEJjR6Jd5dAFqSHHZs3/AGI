代码：0注释。

单文件启动→检查运行环境是否符合要求→运行环境符合要求→空闲；

单文件启动→检查运行环境是否符合要求→运行环境不符合要求→自愈→检查运行环境是否符合要求→运行环境符合要求→空闲；

单文件启动→检查运行环境是否符合要求→运行环境不符合要求→自愈→检查运行环境是否符合要求→运行环境不符合要求→弹窗；

弹窗上的信息：
初检结果、初检后进行的自愈尝试、复检结果、下一步建议；

弹窗上的按钮：
“浏览”：选择雷电模拟器路径；
“更多”：查看详细信息；
“重试”：关闭弹窗→自愈→检查运行环境是否符合要求；
“通过”：关闭弹窗→空闲（意味着用户已自行确认运行环境符合要求）；



只允许用户修改：雷电模拟器路径（默认D:\LDPlayer9\dnplayer.exe）、存储路径（默认C:\Users\Administrator\Desktop\AAA）、经验池上限（默认10GB）、AI模型数量上限（默认10个）、鼠标最长静止时间（默认10秒）、训练模式最长持续时间（默认900秒）。

空闲→用户点击“修改”按钮→用户修改数据→数据处理与保存→空闲；

进度条：在睡眠模式期间，显示进度（0%→100%）

对一个画面进行评分：在经验池中挑选一批与该画面最相似的历史画面，然后进行判断：相似度越高（0%→100%），评分越低（100%→0%）；

空闲→用户点击“学习模式”按钮/用户点击“训练模式”按钮→确保运行环境符合要求→确保控制面板已最小化→确保鼠标在雷电模拟器客户区内→进入学习模式（用户在雷电模拟器客户区内操作鼠标）/进入训练模式（AI在雷电模拟器客户区内操作鼠标）→记录画面（包括但不限于：截图、时间戳、评分）和鼠标（包括但不限于：轨迹、左键、右键、滚轮、操作来源、位置、移动方向、瞬时速率、时间戳）和其他信息→用户按下ESC键/鼠标倒计时归零/鼠标在雷电模拟器客户区外/雷电模拟器客户区异常（不可见，被遮挡，最小化，未完全位于电脑屏幕范围内）→数据处理与保存→退出学习模式/退出训练模式→空闲→电脑屏幕上出现控制面板；

进入训练模式→画面评分持续为0→自救（自救期间无视训练模式最长持续时间限制）→画面评分不再为0→停止自救→重置训练模式倒计时；

睡眠模式任务2需要训练的AI模型（这些AI模型视为1组）：
①画面新颖程度评分模型：一个画面与经验池中和它最相似的一批历史画面相似度越高（0%→100%），评分越低；
②鼠标拟人程度评分模型：AI在训练模式期间输出的鼠标操作，与用户在学习模式期间的鼠标操作相似度越高（0%→100%），评分越高；
③实操模型：在训练模式（非自救）期间，在雷电模拟器客户区内，AI输出鼠标操作；
④自救模型；
⑤奖励模型：画面评分不同的样本，画面评分越高，奖励越高；画面评分相同的样本，鼠标评分越高，奖励越高；
⑥数值变化模型：依据现实条件确定没有在要求中确定的变量的初始数值，并让它们跟随现实条件的变化而变化；

进入训练模式→训练模式倒计时归零→数据处理与保存→退出训练模式→进入睡眠模式→任务1（检查画面评分：对没有评分的画面进行评分，对评分有错误的画面重新评分）→数据处理与保存→任务1完成→任务2（训练AI模型）→数据处理与保存→任务2完成→任务3（检查AI模型组和经验池：当组数量超过上限时，持续删除最旧的组，直至组数量不高于上限；当经验池大小超过上限时，持续删除奖励最低的样本，直至经验池大小不高于上限的50%）→数据处理与保存→任务3完成→退出睡眠模式→空闲→确保运行环境符合要求→确保控制面板已最小化→确保鼠标在雷电模拟器客户区内→进入训练模式；

进入睡眠模式→用户按下ESC键→数据处理与保存→退出睡眠模式→空闲→电脑屏幕上出现控制面板；

空闲→用户点击“睡眠模式”按钮→进入睡眠模式→任务1（检查画面评分：对没有评分的画面进行评分，对评分有错误的画面重新评分）→数据处理与保存→任务1完成→任务2（训练AI模型）→数据处理与保存→任务2完成→任务3（检查AI模型组和经验池：当组数量超过上限时，持续删除最旧的组，直至组数量不高于上限；当经验池大小超过上限时，持续删除奖励最低的样本，直至经验池大小不高于上限的50%）→数据处理与保存→任务3完成→退出睡眠模式→空闲；

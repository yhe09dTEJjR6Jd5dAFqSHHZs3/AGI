代码：0注释。

对于任意一个数值，如果没有在要求中确定，那么它最好由AI根据各种现实条件创生，并跟随现实条件的变化而变化。

单文件启动→检查运行环境是否符合要求；

运行环境不符合要求→自愈；

自愈失败→弹窗显示详细原因→用户点击“确认”按钮→终止程序；

自愈成功→检查运行环境是否符合要求；

运行环境符合要求→空闲；

只允许用户修改：雷电模拟器路径（默认D:\LDPlayer9\dnplayer.exe）、存储路径（默认C:\Users\Administrator\Desktop\AAA）、经验池上限（默认10GB）、AI模型数量上限（默认10个）、鼠标最长静止时间（默认10秒）、训练模式最长持续时间（默认900秒）。

空闲→用户点击“修改”按钮→用户修改数据→数据处理与保存→空闲；

进度条：在睡眠模式期间，显示进度（0%→100%）

画面评分：在经验池中挑选一批与该画面最相似的历史画面，然后进行判断：越新颖（0%→100%），评分越高（0%→100%）；完全相同，评分为0；

空闲→用户点击“学习模式”按钮/用户点击“训练模式”按钮→确保运行环境符合要求→确保控制面板已最小化→确保鼠标在雷电模拟器客户区内→进入学习模式（用户在雷电模拟器客户区内操作鼠标）/进入训练模式（AI在雷电模拟器客户区内操作鼠标）→记录画面（包括但不限于：截图、时间戳、评分）和鼠标（包括但不限于：轨迹、左键、右键、滚轮、操作来源、位置、移动方向、瞬时速率、时间戳）→用户按下ESC键/鼠标倒计时归零/鼠标在雷电模拟器客户区外/雷电模拟器客户区异常（不可见，被遮挡，最小化，未完全位于电脑屏幕范围内）→数据处理与保存→退出学习模式/退出训练模式→空闲→电脑屏幕上出现控制面板；

进入训练模式→画面评分持续为0→自救（自救时长不受训练模式最长持续时间限制）→画面评分不再为0→停止自救→重置训练模式倒计时；

进入训练模式→训练模式倒计时归零→数据处理与保存→退出训练模式→进入睡眠模式→任务1（检查画面评分：对没有评分的画面进行评分，对评分有错误的画面重新评分）→任务1完成→任务2（训练AI模型：对于两个评分不同的画面，画面评分越高，奖励越高；对于两个评分相同的画面，鼠标与用户在学习模式期间的操作越相似，奖励越高）→任务2完成→任务3（检查AI模型和经验池：当AI模型数量超过上限时，持续删除最旧的AI模型，直至AI模型数量不高于上限；当经验池大小超过上限时，持续删除评分最低的数据，直至经验池大小不高于上限的50%）→任务3完成→数据处理与保存→退出睡眠模式→空闲→确保运行环境符合要求→确保控制面板已最小化→确保鼠标在雷电模拟器客户区内→进入训练模式；

进入睡眠模式→用户按下ESC键→数据处理与保存→退出睡眠模式→空闲→电脑屏幕上出现控制面板；

空闲→用户点击“睡眠模式”按钮→进入睡眠模式→任务1（检查画面评分：对没有评分的画面进行评分，对评分有错误的画面重新评分）→任务1完成→任务2（训练AI模型：对于两个评分不同的画面，画面评分越高，奖励越高；对于两个评分相同的画面，鼠标与用户在学习模式期间的操作越相似，奖励越高）→任务2完成→任务3（检查AI模型和经验池：当AI模型数量超过上限时，持续删除最旧的AI模型，直至AI模型数量不高于上限；当经验池大小超过上限时，持续删除评分最低的数据，直至经验池大小不高于上限的50%）→任务3完成→数据处理与保存→退出睡眠模式→空闲；

代码：0注释，基于事件而非基于时间，最好不要有硬编码，

对于任意一个数值，如果没有在AGENTS.md中确定，那么它必须由AI根据各种现实条件创生，并跟随现实条件的变化而变化。

单文件启动→检查运行环境是否符合要求；

运行环境不符合要求→依赖缺失→自动安装缺失的依赖；

依赖安装失败→弹窗警告→用户点击“确认”按钮→终止程序；

运行环境不符合要求→配置文件缺失→自动生成配置文件；

运行环境符合要求→空闲；

除非AGENTS.md中明确允许，否则禁止用户修改内容。

允许用户修改雷电模拟器路径：D:\LDPlayer9\dnplayer.exe

空闲→用户点击“修改”按钮→用户修改雷电模拟器路径→空闲；

允许用户修改存储路径：C:\Users\Administrator\Desktop\AAA

空闲→用户点击“修改”按钮→用户修改存储路径→数据迁移→空闲；

学习模式期间：用户在雷电模拟器上操作鼠标。同时记录画面（截图、时间戳），鼠标（包括但不限于：轨迹、左键、右键、滚轮、操作来源、时间戳）；

训练模式期间：根据雷电模拟器实时画面，AI在雷电模拟器上操作鼠标。同时记录画面（截图、时间戳），鼠标（包括但不限于：轨迹、左键、右键、滚轮、操作来源、时间戳）；

空闲→用户点击控制面板上的“学习模式”按钮→最小化控制面板→进入学习模式；

空闲→用户点击控制面板上的“训练模式”按钮→最小化控制面板→进入训练模式：

学习模式→用户按下ESC键/鼠标长时间保持静止（默认10秒，允许用户修改）/雷电模拟器窗口异常（不可见，被遮挡，最小化，未完全位于屏幕范围内）→退出学习模式→空闲；

训练模式→用户按下ESC键/时间到（默认900秒，允许用户修改）/雷电模拟器窗口异常（不可见，被遮挡，最小化，未完全位于屏幕范围内）→退出训练模式→空闲；

简洁、美观、灵活的控制面板：允许用户缩放，永远呈现完整内容；

空闲→用户点击控制面板上的“睡眠模式”按钮→进入睡眠模式；

睡眠模式→睡眠任务完成/用户按下ESC键/时间到（默认1800秒，允许用户修改）→退出睡眠模式→空闲；

进度条：在睡眠模式/数据迁移期间，实时显示进度（0%→100%）；在学习模式/训练模式期间，实时显示进度（100%→0%）；

奖励：增加“人生阅历”；

对于一个画面，在经验池中挑选一批与之最接近的历史画面，然后进行判断：越新颖（0%→100%），奖励越高（0%→100%）；完全相同，没有奖励；

当画面奖励相同时，比较鼠标操作：与用户在学习模式期间的鼠标操作相似度越高（0%→100%），奖励越高；
